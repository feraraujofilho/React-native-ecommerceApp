
class Product {
    constructor(id, userId, title, imageUrl, bannerImgUrl, description, price) {
        this.id = id,
            this.userId = userId,
            this.title = title,
            this.imageUrl = imageUrl,
            this.bannerImgUrl = bannerImgUrl,
            this.description = description,
            this.price = price
    }
}

export default Product